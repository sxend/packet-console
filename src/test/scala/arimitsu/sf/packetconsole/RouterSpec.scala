package arimitsu.sf.packetconsole

import org.scalatest._

class RouterSpec extends FlatSpec with Matchers {
  "Endpoint" should "return statics" in {
    val method = "GET"
    val path = "/statistics"
  }
}
