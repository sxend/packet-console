package arimitsu.sf.packetconsole

object PropertyKey {
  val PC_API_CREDENTIAL = "packet.console.credential"
  val PC_LISTEN_HOST = "packet.console.host"
  val PC_LISTEN_PORT = "packet.console.port"
}
